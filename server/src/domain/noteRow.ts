export function noteRowToDetail(row: any) { return { id: row.id, title: row.title, slug: row.slug, content: row.content, folderId: row.folder_id, tags: row.tags, links: row.links, createdAt: row.created_at, updatedAt: row.updated_at } }; export function noteRowToListItem(row: any) { return noteRowToDetail(row); }
