export function journalRowToJson(row: any) { return { id: row.id, entryDate: row.entry_date, title: row.title, bodyHtml: row.body_html, bodyText: row.body_text, createdAt: row.created_at, updatedAt: row.updated_at } }
